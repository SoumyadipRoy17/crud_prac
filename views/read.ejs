<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
      <div class="nav mb-10">
        <a href="/" class="text-blue-500 mt-3">Back To Home</a>
      </div>
      <h1 class="text-3xl mb-3 tracking-tighter">All Users</h1>
      <div class="users flex gap-3 flex-wrap">
        <% if(users.length > 0){ %> <% users.forEach(function(user){ %>
        <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
          <div class="w-full h-72 rounded-lg bg-zinc-700">
            <img
              class="w-full h-full object-cover object-top overflow-hidden"
              src="<%= user.image %>"
              alt=""
            />
          </div>
          <h3 class="text-xl tracking-tighter mt-3"><%= user.name %></h3>
          <h5 class="text-xinc-500"><%= user.email %></h5>
          <div class="flex-gap-3">
            <a class="text-zinc-200" href="/edit/<%= user._id %>">Edit user</a>
            <a class="text-red-500" href="/delete/<%= user._id %>"
              >Delete user</a
            >
          </div>
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-zinc-500">No users Yet</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
